# SystemJS — AI working notes

Upstream repo: https://github.com/systemjs/systemjs — dynamic ES module loader (~1.5k LOC src, ES5-style, size-golfed browser payload built with chomp → rollup → terser, plus an ncc-bundled Node build).

Task tracking: `tasks` MCP project **systemjs** (prefix `SJS`).

## Documents

- [ts-conversion.md](./ts-conversion.md) — SJS-1 exploration: converting src/ to TypeScript with real types (branch `explore/typescript-conversion`).
